budget:
  chassis:
    http-logging:
      enabled: true
spring:
  main:
    lazy-initialization: true
    allow-bean-definition-overriding: true
  profiles:
    active: test
  cloud:
    config:
      enabled: false
  security:
    user:
      name: "testUserName"
      password: "testUserPassword"
